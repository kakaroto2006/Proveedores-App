using System;

namespace ProveedoresApp;

public class Program
{
    // This is the entry point for single-project .NET MAUI apps.
    static void Main(string[] args)
    {
        var app = MauiProgram.CreateMauiApp();
        app.Run(args);
    }
}